#include <stdio.h>
#include <stdlib.h>

/**
 * main -Entry point
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{
	int singledgt;

	for (singledgt = 0; singledgt < 10; singledgt++)
		printf("%d", singledgt);
	printf("\n");
	return (0);
}
