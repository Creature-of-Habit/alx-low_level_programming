#include <stdarg.h>
#include <stdio.h>
#include <stdlib.h>

/**
 * print_all - function that prints anything
 * @format: string to be printed containing specifiers
 *
 */

void print_all(const char * const format, ...)
{
	int i = 0;
	va_list args;

	while (format[i++] != '\0')
	{
	}

}
